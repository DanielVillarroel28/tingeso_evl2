server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/DanielVillarroel28/tingeso_evl2.git
          search-paths: config-data
  application:
    name: config-service
